# 🍅 番茄钟桌面 Widget

一个显示番茄钟计时状态的Windows桌面悬浮窗组件。

## 功能特性

- 📱 矩形悬浮窗显示剩余时间
- 🔄 实时同步网页番茄钟状态
- 📡 通过Supabase实时广播
- 📦 系统托盘集成
- 🎨 深色/浅色主题适配

## 前置条件

1. Node.js 18+
2. Rust 1.70+
3. Tauri CLI (`cargo install tauri-cli`)
4. Supabase 项目（用于实时同步）

## 安装步骤

### 1. 配置Supabase

在 [Supabase](https://supabase.com) 创建项目后，复制以下信息：
- Project URL
- anon public key

编辑 `.env` 文件：
```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 2. 安装依赖

```bash
cd desktop-widget
npm install
```

### 3. 安装Tauri依赖

```bash
cargo install tauri-cli
```

### 4. 开发模式运行

```bash
npm run dev
```

### 5. 构建安装包

```bash
npm run tauri build
```

## 在网页端使用

确保你的番茄钟网页已登录Supabase，Widget会自动同步计时器状态。

## 项目结构

```
desktop-widget/
├── src/
│   ├── components/     # React组件
│   ├── lib/           # Supabase客户端
│   ├── App.tsx        # 主应用
│   └── main.tsx       # 入口
├── src-tauri/         # Tauri Rust源码
├── icons/            # 图标资源
└── package.json
```

## 与网页端同步

Widget通过Supabase Realtime Broadcast与网页端通信：
1. 网页端在计时器状态变化时广播消息
2. Widget订阅广播频道并实时更新显示
3. 双方使用本地存储的用户ID进行区分

## 快捷操作

- 点击悬浮窗可拖动
- 点击"打开番茄钟"在浏览器中打开网页
- 系统托盘右键菜单可快速访问
